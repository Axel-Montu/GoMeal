<div class="form-input-go">
  <label for="<%= id %>"><%= label %></label>

  <select id="<%= id %>">
    <% options.each do |option| %>
      <option><%= option %></option>
    <% end %>
  </select>
</div>

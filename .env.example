# Google Places — used by db/seeds.rb to fetch restaurants
GOOGLE_PLACES_API_KEY=

# OpenRouteService — sign up at https://account.heigit.org/signup
# One account and one key per person. Never commit the value.
ORS_API_KEY=
